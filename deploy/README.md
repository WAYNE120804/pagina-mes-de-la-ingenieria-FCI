# Deploy

Archivos base para publicar la plantilla con Docker Compose y Caddy.

Antes de desplegar, reemplaza los dominios de ejemplo en `deploy/Caddyfile` y ajusta las variables de entorno del entorno destino.
